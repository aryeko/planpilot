query FetchRepo($owner: String!, $name: String!) {
  repository(owner: $owner, name: $name) {
    id
    labels(first: 100) {
      nodes { id name }
    }
    issueTypes(first: 100) {
      nodes { id name }
    }
  }
}
